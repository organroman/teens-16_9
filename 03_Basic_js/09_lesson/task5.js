/**
 * Завдання
 *
 * Написати функцію, яка повертає найбільше число, з переданих їй як аргументи при виклику.
 *
 * Генерувати помилку, якщо:
 * - Якщо функція була викликана менше, ніж з двома аргументами;
 * - Хоч один з аргументів не є допустимим числом (у помилці вказати його порядковий номер).
 *
 * Умови:
 * - Використовувати тип функції arrow function;
 * - Використовувати об'єкт arguments заборонено;
 * - Обов'язково використовувати об'єкт Math.
 */

